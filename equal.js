
    
    var chec = checkEqual(1, 1);
    console.log(chec);

    function checkEqual(a, b) {
        return a === b ? "Equal" : "Not Equal";
      }

      var answer = checkEqual(1,2);
      console.log(answer);